import { describe, test, expect } from "vitest";
import { fireEvent, screen, render } from "solid-testing-library";
import { createCodeMirror } from "../src/createCodeMirror";

describe("createCodeMirror", () => {
  test("test", () => {});
});
